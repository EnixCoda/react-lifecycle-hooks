{
  "name": "react-lifecycle-hooks",
  "version": "0.1.1",
  "keywords": [
    "react",
    "lifecycle",
    "hook",
    "listen",
    "middleware"
  ],
  "description": "Add hooks for lifecycles of all react components in your app easily.",
  "main": "lib/index.js",
  "repository": "https://github.com/EnixCoda/ReactLifecycleHooks",
  "author": "EnixCoda",
  "license": "MIT",
  "scripts": {
    "build": "rollup src/index.js --file lib/index.js --format umd --name \"react-lifecycle-hooks\"",
    "prepublish": "npm run build"
  },
  "peerDependencies": {
    "react": "^16.0.0"
  },
  "devDependencies": {
    "rollup": "^0.65.0"
  },
  "dependencies": {
    "compare-versions": "^3.4.0"
  }
}
